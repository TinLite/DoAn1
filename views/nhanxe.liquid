{% layout "_include/layout.liquid" %}
{% block content %}
    {% if err %}
        <div class="text-center py-4 bg-rose-600 text-white text-2xl">{{ err }}</div>
    {% endif %}
    <div class="container mx-auto p-4 max-w-2xl">
        <div class="text-xl py-2 border-b-2 border-gray-900 border-solid">Nhận xe</div>
        <form
            action=""
            method="post"
            autocomplete="off"
            class="grid grid-cols-1"
            id="thembai">
            <label for="maphieu" class="my-2">Mã phiếu</label>
            <input
                type="text"
                name="maphieu"
                id="maphieu"
                value="{{ body.maphieu}}"
                placeholder="1234">
            <label for="soxe" class="my-2">Số xe</label>
            <input
                type="text"
                name="soxe"
                id="soxe"
                value="{{ body.soxe }}"
                placeholder="65-AA12222">
            <button type="submit" class="block bg-black text-white px-4 py-2 mt-2">Thêm</button>
        </form>
        <div class="text-xl py-2 border-b-2 border-gray-900 border-solid">Lịch sử hệ thống</div>
        <table class="border-collapse border w-full text-center">
            <thead class="bg-gray-100 border-b border-gray-300 pt-4">
                <tr class="">
                    <th class="">Mã phiếu</th>
                    <th class="">Số xe</th>
                    <th class="">Vị trí bãi đỗ</th>
                    <th class="">Thời gian vào</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-gray-300">
                {% for nhanxe in danhsachnhanxe %}
                    <tr class="hover:bg-slate-100">
                        <td>{{ nhanxe.Phieu }}</td>
                        <td>{{ nhanxe.Soxe }}</td>
                        <td>cc</td>
                        <td>{{ nhanxe.Thoigianvao | date: "%d tháng %m %H:%M" }}</td>
                    </tr>
                {% else %}
                    <tr class="bg-slate-100">
                        <td colspan="4">{{ "Không tìm thấy dữ liệu xe nào." }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}